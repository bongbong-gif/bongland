# Render 재배포

GitHub 저장소의 `aooni-web-multiplayer-v04` 폴더로 파일을 교체한 뒤 기존 Render Web Service를 재배포합니다.

현재 서비스 설정을 그대로 유지하면 됩니다.
- Root Directory: `aooni-web-multiplayer-v04`
- Build Command: `npm install`
- Start Command: `npm start`
